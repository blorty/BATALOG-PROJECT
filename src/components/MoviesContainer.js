import React from "react";
import ContainerCard from "./ContainerCard";

//create a Container component that will render ContainerCard components
function Container (movies) {
    //create a variable list that will map through the movies array and render a ContainerCard component for each movie
    // const moviesList = movies.map((movie) => {
    //     return 
    //     <ContainerCard 
    //         key={movie.id} 
    //         movie={movie} 
    //     />;
    // });
};

export default Container;